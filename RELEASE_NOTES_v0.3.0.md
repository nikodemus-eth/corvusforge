## Corvusforge v0.3.0 — Production Release

**Deterministic, Auditable, Sovereign Coding Pipeline**

### What's New
- **Production Hardening** — Docker + docker-compose, GitHub CI/CD, env-driven config
- **Full Multi-Agent UI** — Dark Streamlit dashboard with live Thingstead telemetry
- **DLC Marketplace** — Local-first signed plugin distribution
- **SAOE + Thingstead** — Every run is a sovereign fleet with persistent `.openclaw-data`

### Assets
- `corvusforge-0.3.0-py3-none-any.whl` — Install with `pip install`
- `corvusforge-0.3.0.tar.gz` — Source
- Docker image: `docker build -t corvusforge:0.3.0 .`

### Getting Started (Private Repo)
1. `pip install corvusforge==0.3.0`
2. `corvusforge ui`
3. `corvusforge new`
4. Drop DLCs into `plugins/`

**Repository**: Private by default (change to public later).

Built by nikodemus.crypto — Corvusforge is now production-ready.
